# Build SAM application

Build all Lambda functions in parallel using the SAM build cache.

```bash
sam build --parallel --cached
```

Report what was built and any build errors. If the build fails, read the relevant source files and explain the error with suggested fixes.
