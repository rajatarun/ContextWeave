# Validate SAM template

Run SAM template validation with linting to catch CloudFormation issues before deployment.

```bash
sam validate --lint
```

Report any errors found. If validation passes, confirm the template is valid and ready for deployment. If there are warnings or errors, explain what each issue means and suggest fixes.
